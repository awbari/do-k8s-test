apiVersion: v1
kind: Service
metadata:
  name: test-app
  namespace: test
  annotations:
    external-dns.alpha.kubernetes.io/hostname: echo.nalut.de
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 80
  selector:
    app: test-app
